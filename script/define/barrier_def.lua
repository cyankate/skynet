--[[
    主线关卡（barrier）常量。
]]

local RECOVERY_ENUM = require "setting.RECOVERY_ENUM"

return {
    STAMINA_RECOVERY_ID = RECOVERY_ENUM.CAPACITY,
    STAMINA_COST = 1,
    MAX_STARS = 3,
    MIN_BARRIER_ID = 1,
}
